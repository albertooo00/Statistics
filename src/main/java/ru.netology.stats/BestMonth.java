package ru.netology.stats;

public class BestMonth {
    public long findMax(int[] values) {
        int month = 0;
        for (int value : values) {
            month = month + 1;
            if(month > value);

        }
        return month;
    }
}